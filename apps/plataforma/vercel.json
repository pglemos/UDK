{
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "(cd ../.. && pnpm verify && pnpm lint && pnpm typecheck && pnpm test) && pnpm build"
}
